import Foundation

print("Welcome to the tip calculator.")

func tipsCalculator() {
    let normalBill : Int
    let personNumber  = 0
    let tipsPercentage  = 0

    print("What was the total bill?")
    //normalBill = Int(readLine())
    print("How many people to split the bill?")
    //personNumber = Int(readLine())
    print("What percentage tip would you like to give? 10%, 12%, or 15%?")
    //tipsPercentage = Int(readLine())

}